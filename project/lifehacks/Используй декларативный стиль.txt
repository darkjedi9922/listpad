Так в отладчике проще увидеть действительные значения
int width = widgetA.width();
int heigh = widgetB.height();
widgetC.resize(width, height);

, чем так:
widgetC.resize(widgetA.with(), widgetB.height());

Можно, конечно, потом переписать в декларативный стиль, но это время.
Плюс, есть стимул выделить это в отдельную функцию, отчего будет еще понятней, что происходит:

resizeWidgetC();